# Zsh aliases

alias ls='ls -a -l --almost-all --si' # Show all when normal ls is used

alias mkdir='mkdir -p' # Make missing parent directories when necessary

# Shortcuts

alias v='nvim'
alias g='git'
alias r='ranger'
